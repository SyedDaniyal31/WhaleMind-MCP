# WhaleMind MCP Server — env vars

# Required for Etherscan (rate limits without it)
ETHERSCAN_API_KEY=

# Optional: WhaleMind Flask API for verdict/balance
WHALEMIND_API_URL=

# Optional: Context JWT audience (e.g. https://your-app.up.railway.app/mcp)
MCP_ENDPOINT_URL=

# Dev only: bypass JWT for tools/call (NEVER set in production)
# SKIP_CONTEXT_AUTH=true
